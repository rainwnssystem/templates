apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: <APP>-pvc
  namespace: <NAMESPACE>
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs-sc
  resources:
    requests:
      storage: 4Gi
